mixin dropdown(items)    .custom-dropdown        .select            span.selected Выберите город            .caret        ul.custom-dropdown-menu            each item,index in items                if(item.selected)                    li.active=item.name                else                    li=item.name